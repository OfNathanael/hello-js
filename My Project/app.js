//import {add, subtract} from './math.js';
//import can be used instead of const.
const math = require('./math'); //Imports the math module, which contains the add and subtract functions defined in math.js.

console.log(math.add(7, 15)); //Calls the add function from the math module with the arguments 3 and 15, and logs the result to the console.

console.log(math.subtract(451, 655)); //Calls the subtract function from the math module with the arguments 451 and 655, and logs the result to the console. Note: there is a typo in "console" which should be corrected to avoid errors.